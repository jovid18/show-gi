# apps/web과 그 툴체인 검증. 로컬에서 `pnpm lint && pnpm typecheck && pnpm test && pnpm build`와 같다.
#
# lint는 루트에서 돈다 — oxfmt가 마크다운까지 포맷하므로 docs와 README도 여기서 검사된다.

name: Web

on:
  push:
    branches: [main]
  pull_request:

# 체크아웃에 필요한 것만 준다. 레포 기본값도 현재 read지만 그건 설정이라
# 언제든 바뀌고 포크에는 따라가지 않는다. 파일에 박아두는 쪽이 실제 보증이다.
permissions:
  contents: read

jobs:
  # job 이름이 그대로 필수 체크의 컨텍스트가 된다. server.yml과 겹치면 룰셋이
  # 어느 쪽을 가리키는지 알 수 없어 체크가 영원히 대기 상태로 남는다.
  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm build
