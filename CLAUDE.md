# CLAUDE.md

개입할 때를 스스로 정하는 쇼기 상대. 마감 없이 계속 다듬는 사이드 프로젝트다.

설계는 [docs/](docs/README.md)에 있다. 하나만 읽는다면 [docs/01-core.md](docs/01-core.md).

루트 `README.md`는 개발자를 위한 한국어 문서다. 일본어로 작성했을 때는 내용을 수정할 때마다 `docs/`와 두 벌을 맞춰야 했다. 일본어는 UI 문자열과 쇼기 용어에만 쓴다. 개발 문서 인덱스는 [docs/README.md](docs/README.md)다.

작업을 이어받는다면 [docs/06-status.md](docs/06-status.md)를 먼저 읽는다. §1의 「지금 실제로 도는 것」 표에서 구현 여부를, §5에서 다음 작업을 확인한다. 상태가 바뀌면 같은 PR에서 그 문서를 갱신한다.

측정과 결정은 [docs/journal/](docs/journal/README.md)에 절 번호를 붙여 기록한다. `06-status.md`에는 결과만 담는다. 과거에는 상태 문서가 575KB까지 커져 갱신하기 어려웠다. 코드 주석 500여 곳이 `journal §NN`으로 참조하므로 절 번호는 다시 매기지 않는다.

새 사실을 추가할 때는 그 사실과 모순되는 옛 문장도 같은 PR에서 고친다. 모순이 남으면 읽는 절에 따라 결론이 달라지고, 문서를 대조해도 어느 쪽이 맞는지 알 수 없다. 이런 문제가 네 번 있었고([§29](docs/journal/21-40.md)), 병렬 작업에서도 반복됐다. 두 번째 병렬 작업에서는 세 곳이 어긋났다([§36](docs/journal/21-40.md)). 작업이 끝나면 문서 PR 하나로 통합한다.

|               |                                                          |
| ------------- | -------------------------------------------------------- |
| `apps/server` | Go. HTTP·WebSocket·USI 엔진 브리지. `go.mod`가 여기 있다 |
| `apps/web`    | Vite + React + three.js 클라이언트. pnpm 워크스페이스    |

## 이 프로젝트의 요점

판단과 표현은 모두 결정적이어야 한다. 블런더 여부와 카테고리는 결정적 룰과 엔진 평가치로 정하고, 화면에 나가는 일본어도 그 사실에서 코드가 조립한다(`internal/explain`). 초심자는 설명의 오류를 스스로 검증하기 어려워 잘못된 내용을 그대로 배울 수 있다.

생성 모델을 판단과 표현에 쓰지 않는다. 한때 LLM으로 개입 문구를 생성했지만 제거했다. 이 프로젝트에서는 자연스러운 표현보다 같은 사실에 같은 문장이 나오는 것을 우선한다. 판 전체를 프롬프트에 넣고 수를 평가하게 하는 방식도 허용하지 않는다.

LLM은 아래 두 곳에서 입력을 옮겨 적는 데만 쓴다. 판단이나 설명 생성은 맡기지 않는다.

| 어디                                                   | 하는 일                                           |
| ------------------------------------------------------ | ------------------------------------------------- |
| `internal/kifunorm` ([§126](docs/journal/121-140.md))  | 가져온 기보의 서식 정규화 — 글자를 옮겨 적는다    |
| `internal/boardread` ([§129](docs/journal/121-140.md)) | 판이 찍힌 그림의 판독 — 격자를 그려진 대로 적는다 |

LLM에 좌표 변환을 맡기지 않는다. 서양식 표기(P-7f)를 일본어로 옮기는 시험에서 13手 중 하나의 rank 글자를 잘못 읽었다. 세 번 실행해 모두 같은 자리에서 틀렸다. 프롬프트에는 筋·段·「5五」를 쓰지 않는다. 그림도 위 줄부터 왼쪽부터 그려진 대로 읽게 한다. 이 순서가 SFEN 판 칸의 순서와 같으므로 옮기는 코드에 좌표 계산이 없다. 先手·後手 판단도 맡기지 않는다. 사진에서는 위쪽 편과 아래쪽 편만 구분하고, 아래쪽을 先手로 두는 것은 코드가 정한다.

검증 방식은 입력에 따라 다르다. 기보는 옮긴 표기 전부를 룰 엔진으로 검증한 뒤 수로 받아들인다(`internal/kifu` → `shogi.ValidateMove`). 그림에는 재생할 수순이 없으므로 `shogi.Faults`로 국면 자체를 검사한다(二歩·玉 수·行き所のない駒·王手放置). 이 검사로 잡지 못하는 오독은 사람이 확인 화면에서 고친다. 銀을 成銀으로 잘못 읽어도 합법인 국면은 코드로 걸러낼 수 없으므로 확인 화면을 제거하지 않는다.

결정적 파서가 하나라도 성공하면 LLM 정규화를 호출하지 않는다. 같은 기보에 같은 결과를 내는 파서를 우선하고, 파서가 실패한 경우에만 정규화한다.

카테고리마다 설명에 쓸 수 있는 사실이 다르다(`explain.Facts.used`). 문구를 고치기 전에 그 목록을 확인한다. 근거 없는 설명을 막기 위해 목록에 없는 사실은 문장에 넣지 않는다.

개입 판정은 엔진에 의존하지 않는다. `internal/intervene`의 입력은 이미 구해진 평가치와 詰み 거리뿐이다. 상수(K·임계치)는 엔진 없이 조정·검증할 수 있어야 하며, 실측으로 정한다.

대국 상태는 세션 goroutine 하나가 소유한다. 롤백을 포함한 상태 변경 순서를 보장해야 하므로 HTTP 핸들러는 상태를 직접 읽지 않는다.

엔진 탐색은 무조건 깊이(`go depth`)로 건다. 시간(`go movetime`)을 쓰지 않는다.

시간 기반 탐색은 같은 국면에서도 결과가 달라진다. `positions` 캐시의 "같은 국면 = 같은 결과" 전제와 `computed_depth`가 의미를 잃는다. 후보 평가치도 부하에 따라 달라져 밴드 제어가 성립하지 않고, 상대의 강함이 서버 사정에 따라 바뀐다.

탐색을 중간에 끊어 결과를 쓰지 않는다. 완료되지 않은 depth N 탐색을 `computed_depth = N`으로 저장하면 개입 판정이 잘못된 깊이의 캐시를 사용하게 된다.

플레이어를 기다리게 해도 된다. 지연은 캐시·선행 계산·탐색 깊이 조정으로 줄인다. 시간 기반 탐색을 방지하기 위해 `usi` 패키지에는 해당 API를 두지 않는다.

현재 여섯 곳의 실행 깊이는 모두 12이며 `game.DefaultDepth`를 공유한다([§140](docs/journal/121-140.md)). [§130](docs/journal/121-140.md)에서 14로 올렸다가 지연 때문에 되돌렸다. 手合割 기준점 표는 depth 14 실측이라 다시 재기 전까지 `[미확정]`이다. 깊이를 올리면 `positions` 캐시는 전부 무효가 된다(`computed_depth`). 내릴 때는 깊게 잰 행이 그대로 히트한다.

## 검증

```sh
pnpm lint && pnpm typecheck && pnpm test && pnpm build   # 루트. 웹 전체 + 마크다운 포맷
cd apps/server && gofmt -l . && go vet ./... && go test -race ./...
terraform -chdir=infra fmt -check && terraform -chdir=infra validate
```

DB·엔진 테스트는 환경변수가 없으면 경고 없이 건너뛴다. `go test`가 성공해도 이 테스트들을 실행한 것은 아니다. 필요한 환경변수는 [apps/server/README.md](apps/server/README.md)의 표를 확인한다.

```sh
docker compose up -d db
for f in apps/server/internal/store/migrations/*.sql; do   # 번호 순서대로 전부
  docker exec -i show-gi-db psql -U showgi -d showgi -v ON_ERROR_STOP=1 < "$f"
done
SHOWGI_TEST_DATABASE_URL='postgres://showgi:showgi@localhost:5432/showgi' go test -race ./...
```

**스키마나 질의를 고치면 `go tool sqlc generate`.** `internal/store/db/`는 생성물이라 손으로 고치지 않는다.

마이그레이션은 자동 배포에서 실행하지 않는다. 파일을 `internal/store/migrations/`에 넣어 PR로 올리고, 실행은 DB 클라이언트로 사람이 직접 한다. `migration` 라벨은 경로에 따라 자동으로 붙는다. PR 본문에 실행해야 할 파일명을 적는다. 절차는 [deploy/README.md](deploy/README.md) §4에 있다.

로컬에서 앱을 띄울 때는 `docker compose up -d`(api + db) 후 `pnpm dev`를 실행한다. `../shogi` 컨테이너가 8080을 사용 중이면 그쪽을 먼저 내린다.

`-race`를 빼지 않는다. 엔진 프로세스와 세션 goroutine이 동시에 실행되므로 데이터 경합을 검사해야 한다.

## PR

**PR은 `/create-pr` 스킬로 만든다.**

`git-workflow:create-pull-request`는 회사(Ecube Labs) 템플릿이라 Jira 링크와 한글 섹션이 들어간다. 이 레포에서는 쓰지 않는다.

**문서 수정이나 사소한 변경은 별도 PR로 쪼개지 않는다.** 작업 중에 나온 문서·설정 수정은 진행 중인 PR에 같이 넣는다. 혼자 쓰는 레포에서 PR을 나누는 이득보다 따로 머지하는 비용이 크다.

한 워크트리에서 브랜치를 여러 개 만들지 않는다. 브랜치를 바꾸면 아직 머지되지 않은 `.gitignore` 규칙이 없어져 `git add -A`가 무시해야 할 파일까지 포함할 수 있다. 병렬 작업은 아래 절차에 따라 워크트리를 나눈다.

## 워크트리 — 병렬로 일할 때

**`/worktree` 스킬로 만든다.** 사람이 여러 세션을 동시에 열어 각자 PR을 내는 것이 이 레포의 기본 진행 방식이다.

디렉터리·브랜치·컨테이너 이름은 같은 slug로 정한다. 이름만 보고 서로의 관계를 알 수 있어야 한다.

| slug `mate-gauge` |                                 |
| ----------------- | ------------------------------- |
| 브랜치            | `feat/mate-gauge`               |
| 디렉터리          | `~/personal/show-gi-mate-gauge` |
| compose 프로젝트  | `show-gi-mate-gauge`            |
| api 컨테이너      | `show-gi-mate-gauge-api`        |

분기는 항상 최신 `origin/main`에서 한다. 다른 작업의 미완성 커밋이 PR diff에 섞이지 않도록 워크트리끼리 분기하지 않는다.

**워크트리에서 일을 시작하면 `WORKTREE.md`를 먼저 읽는다.** 그 워크트리가 맡은 범위와 건드리면 안 되는 파일(다른 워크트리가 이미 점유한 것)이 거기 있다. gitignore 대상이라 커밋되지 않는다.

워크트리별로 수정할 파일이 겹치지 않게 나눈다. 파일이 겹치면 리베이스 비용이 병렬 작업으로 절약한 시간보다 커진다.

`docs/06-status.md` §1 요약표는 마지막에 머지되는 작업에서 통합해 고친다. 담당 없이 각자 수정하다 세 번 어긋난 사례가 있다([§36](docs/journal/21-40.md)).

### 공유하는 것과 아닌 것

모든 워크트리가 db 컨테이너 `show-gi-db`와 데이터베이스 `showgi`를 공유한다. api는 워크트리마다 컨테이너로 실행한다. 엔진이 arm64 Debian 바이너리여서 macOS에서 직접 실행할 수 없기 때문이다.

```sh
# 워크트리에서 (--no-deps 없이 돌리면 컨테이너명 충돌로 실패한다)
docker compose up -d --no-deps api
SERVER_ORIGIN=http://localhost:8081 pnpm dev --port 5174
```

포트는 `n`번째 워크트리가 api `8080+n` · vite `5173+n`. 값은 그 워크트리의 `.env`에 적히고 compose가 알아서 읽는다.

> `a network with name show-gi-net exists but was not created for project ...` 경고는 정상이다. 네트워크를 메인이 만들고 워크트리가 얹는 구조라 나오는 것이고, 붙는 데는 지장이 없다.

DB를 공유하므로 되돌릴 수 없는 마이그레이션을 병렬로 실행하지 않는다. 컬럼·테이블 추가는 다른 워크트리의 서버에 영향을 주지 않는다. `DROP`·`RENAME`·`NOT NULL` 추가는 다른 서버에 오류를 일으키므로 워크트리를 나누지 말고 단독으로 작업한다.

## Git 금지 사항

**머지하지 않는다.** `main` 머지는 사람이 GitHub PR 화면에서 직접 한다. 코딩 에이전트는 PR을 여는 데까지만 작업한다. 머지 여부를 묻지 않고 PR URL을 알려준 뒤 멈춘다.

- `gh pr merge` 금지
- `main`으로의 `git merge` · `git push` 금지
- `git-workflow:merge-to-develop` 금지 (회사 워크플로우. 이 레포엔 `develop`이 없다)

**force push하지 않는다.** `--force`도 `--force-with-lease`도 안 된다. 이미 푸시한 커밋을 되돌릴 때는 `git revert`로 새 커밋을 쌓는다.

**`main`에서 커밋하지 않는다.** 작업은 항상 브랜치를 파서 한다.

GitHub 룰셋은 `main` 직접 푸시, `main` 삭제, 모든 force push, CI(`Server` / `Web`) 통과 전 머지를 막는다. public 레포의 서버 측 브랜치 보호이므로 `--no-verify`로 우회할 수 없다.

`Terraform` 워크플로는 아직 필수 체크에 포함되지 않았다. 사람이 GitHub 화면에서 룰셋에 추가하기 전까지는 이 워크플로가 실패해도 머지가 막히지 않는다.

로컬에는 `.githooks/pre-commit` 하나만 둔다. `main` 커밋을 막고 스테이징된 파일을 포맷한다.

클론마다 한 번:

```sh
git config core.hooksPath .githooks
git config pull.ff only
```

## 커밋

- Conventional Commits (`type: description`)
- **메시지는 영어.** 본문은 `-` 불릿으로 최대 4줄
- 브랜치는 `<type>/<kebab-case>`. 항상 최신 `main`에서 분기

## 포맷

TS·마크다운은 oxfmt, Go는 gofmt, `.tf`는 terraform fmt를 쓴다. Prettier는 쓰지 않는다. 전역 VS Code 설정의 Prettier(100칸)와 oxfmt(120칸)가 서로 다른 줄바꿈을 적용하기 때문이다. `.vscode/settings.json`이 이 워크스페이스의 포매터를 지정하고, `pre-commit`이 스테이징된 파일을 한 번 더 정리한다.

`pnpm lint`는 `.tf`를 검사하지 않는다. oxfmt가 지원하지 않는 확장자이므로 `pre-commit`과 `Terraform` 워크플로에서 검사한다([§90](docs/journal/82-100.md)). terraform CLI가 없으면 훅은 경고 없이 건너뛴다. 이 경우 검사는 CI에서 실행된다.

표의 파이프가 원문에서 어긋나 보여도 손으로 맞추지 않는다. oxfmt가 CJK 문자 폭을 1칸으로 세어 생기는 현상이며 렌더 결과는 정상이다. 수동 정렬은 다음 저장에서 되돌아간다.

## 비밀

`.env`는 커밋하지 않는다. 레포가 퍼블릭이다. 특히 조심할 것:

- Google OAuth 클라이언트 시크릿
- Terraform state — DB 비밀번호가 평문으로 들어간다 (그래서 S3 백엔드에 둔다)
- `../shogi`에서 파일을 가져올 때 `server/.env` · `deploy/.env.prod` · `terraform.tfstate`가 딸려오지 않게 할 것

## 언어 — 사용자에게 보이는 것은 전부 일본어

타깃 사용자는 일본인이다. 화면에 나가는 문자열에는 한글을 넣지 않는다.

| 일본어                                  | 한글                        | 영어           |
| --------------------------------------- | --------------------------- | -------------- |
| UI 문자열 전부 (버튼·라벨·에러·빈 상태) | 문서 본문 (`docs/`, README) | 커밋 메시지    |
| 블런더 설명 문구, 카테고리 이름         | 코드 주석                   | PR 본문        |
|                                         | 루트 README                 | 변수·함수 이름 |

- **`explain`의 문구에 한글을 넣지 않는다.** 문자열이 그대로 화면에 나가므로 `TestNoKoreanReachesTheUser`가 모든 카테고리를 검사한다
- **`../shogi/src/data`는 한 줄도 쓰지 않는다.** 원전이 개인 블로그이고 手筋 208문은 시판 서적을 디지털화한 자료다. 퍼블릭 레포에 사용하면 저작권 문제가 있고, 검증하지 않은 설명을 초심자에게 제공해서도 안 된다
- 쇼기 용어는 문서에서도 일본어 원문 그대로 쓴다 (持ち駒, 成り, 詰み). 한글로 옮기면 검색이 되지 않고 원전과 대조할 수 없다

## 문서

- 확인하지 못한 항목은 추정으로 메우지 말고 `[미확정]`으로 표시
- 새 절은 [docs/journal/](docs/journal/README.md)의 마지막 파일 끝에 붙이고, 목차(`journal/README.md`)에 한 줄 더한다. 번호는 이어서 매긴다
- **저널에는 실측 표와 결정 사항을 적는다.** 결정에 이르는 과정은 적지 않는다. 절 크기가 §6-20의 71줄에서 §33-40의 132줄로 두 배가 되면서 `06-status.md` §1·§5를 갱신할 때 대조할 내용도 늘었다. 저널이 길어지면 상태 문서를 갱신하기 어려워진다
- **같은 문장을 주석과 저널에 두 벌 쓰지 않는다.** 주석에는 코드의 설계 이유를 적고, 실측 숫자와 사례는 `journal §NN`으로 참조한다. 저널 파일을 다시 나눠도 참조가 유지되도록 파일 이름은 적지 않는다. 내용을 복제하면 한쪽만 수정되어 서로 어긋날 수 있다

  조사 당시 서버 주석 3,124줄 중 225줄이 `docs/`의 복사본이었다. 삭제 여부는 길이와 무관하게 아래 기준으로 정한다.

  - 20자 이상이 `docs/`·`CLAUDE.md`와 겹치면 그 문장을 지우고 §NN으로 가리킨다. 인용을 달아 놓고 본문까지 적는 것이 가장 흔한 실패다
  - **경위를 적지 않는다.** 「처음에는 X였는데」·「한 번 보류했다」로 시작하는 문단은 지운다
  - **지우기 전에 다른 기록이 있는지 확인한다.** 그 숫자가 `docs/`에 없으면 유일한 기록이므로 남긴다. 중복을 제거하되 사실은 보존한다
  - **길이와 무관하게 남기는 것**: 부호 뒤집기(先手/後手 관점), 순서 의존, 잠금 규약, goroutine 소유권, panic 위험, 캐시 키에 영향을 주는 것
  - **doc 주석과 선언 사이에 빈 줄을 넣지 않는다.** godoc이 다른 선언에 연결될 수 있으며, 컴파일러와 테스트는 이를 검출하지 못한다

- **주석은 짧은 평서문으로 쓴다.** 긴 문장과 불필요한 수사는 요점을 찾기 어렵게 한다. 조사 당시 서버 주석 5,138줄 중 1,590줄에 마크다운 굵게가 있었고, 그중 상당수는 불필요했다. 아래 다섯 기준을 따른다.

  - **렌더링하지 않는 주석에는 마크다운을 쓰지 않는다.** Go·Terraform·`query/*.sql`(sqlc가 godoc으로 옮긴다)이 해당한다. 굵게와 백틱은 서식 없이 노출되고, 마크다운 링크는 저널 파일을 나눌 때 깨진다. TS의 `/** */`는 에디터가 렌더링하므로 백틱은 남기되 강조는 한 블록에 하나까지다. 적용된 마이그레이션은 주석도 고치지 않는다
  - **결론을 먼저 평서문으로 쓴다.** 「\~것이 요점이다」·「\~이 설계의 전부다」·「\~라는 뜻이다」로 끝내지 않는다
  - **`—` 뒤에 이유를 붙이는 문장을 반복하지 않는다.** 다음 문장으로 나누거나 괄호로 넣는다. 조사 당시 Go 주석 블록 4,374개 중 1,784개(41%)가 `—`를 쓰고 있었다. 같은 문장 구조의 반복을 줄인다
  - **한 문단은 세 줄까지.** 넘으면 대개 두 가지를 한 자리에서 말하고 있는 것이다
  - **doc 주석의 첫 줄에는 기능을 설명한다.** 설계 이유는 빈 줄 뒤에 적는다

- **굵게는 머리말 자리에만 쓴다.** 불릿 기호 뒤, 단락 첫 글자, 표 칸의 시작에서 보이는 길이 35자까지 허용한다. 문장 중간과 제목·인용 안에는 쓰지 않는다. 조사 당시 문서의 굵게 7,548곳 중 3,905곳이 문장 중간에 있었다. 강조가 잦으면 본문을 읽기 어렵다. 정의 목록에서는 항목마다 강조가 필요할 수 있으므로 개수보다 위치를 기준으로 검사한다. `python3 tools/prose/bold.py`로 검사하고 `--fix`로 제거한다

- **다이어그램 원본은 `docs/diagrams/*.drawio`다.** `docs/diagrams/build.sh`가 `docs/images/`로 내보낸다. SVG를 직접 수정하면 다음 빌드에 덮어쓰므로 원본을 고친다. 밝은 테마와 어두운 테마 두 벌을 만들고 README의 `<picture>`가 GitHub 테마에 맞게 선택한다. `light-dark()`는 OS 설정을 따라 GitHub 테마와 어긋나므로 쓰지 않는다. 필요한 CLI는 `brew install --cask drawio`로 설치한다

- **`회차 N #M`은 플레이테스트 회차의 항목 번호다.** 회차와 대국은 [docs/playtests/README.md](docs/playtests/README.md)의 표에서 확인한다. `journal §NN`과 같은 규약으로 문서 이름은 적지 않는다

- **취소선은 다음 개정에서 지운다.** 변경 경위는 저널에 기록하고, 요약표와 미결 목록에는 현재 유효한 문장만 남긴다
