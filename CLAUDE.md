# CLAUDE.md

개입할 때를 스스로 정하는 쇼기 상대. 해커톤 제출작이고 마감이 **2026-08-15 15:00**이다.

설계는 [docs/](docs/)에 있다. 하나만 읽는다면 [docs/01-core.md](docs/01-core.md).

**작업을 이어받는다면 [docs/06-status.md](docs/06-status.md)를 먼저 읽는다** — 지금 무엇이 떠 있고, 무엇이 아직 검증되지 않았고, 다음 순서가 무엇인지가 거기 있다. 상태가 바뀌면 같은 PR에서 그 문서를 갱신한다.

|               |                                                          |
| ------------- | -------------------------------------------------------- |
| `apps/server` | Go. HTTP·WebSocket·USI 엔진 브리지. `go.mod`가 여기 있다 |
| `apps/web`    | Vite + React + three.js 클라이언트. pnpm 워크스페이스    |

## 이 프로젝트의 요점

**판단은 엔진, 표현만 LLM.** 블런더인지 아닌지, 카테고리가 무엇인지는 **결정적 룰과 엔진 평가치**로 정한다. LLM은 그렇게 나온 구조화 데이터를 문장으로 바꾸는 일만 한다. 학습 앱에서 설명이 틀리면 초심자는 검증할 능력이 없어 틀린 것을 그대로 배운다.

판을 통째로 프롬프트에 넣고 "이 수 어때?"를 물어보는 코드가 들어오는 순간 이 레포의 이유가 사라진다.

**대국 상태는 세션 goroutine 하나가 소유한다.** 롤백이 있는 이상 상태 변경 순서가 곧 제품 정합성이다. HTTP 핸들러가 상태를 직접 읽기 시작하면 그 순간 구조가 무너진다.

**엔진 탐색은 무조건 깊이(`go depth`)로 건다. 시간(`go movetime`)을 쓰지 않는다.**

시간 기반은 같은 국면이 같은 답을 주지 않고, 그 하나가 셋을 동시에 무너뜨린다 — `positions` 캐시가 "같은 국면 = 같은 결과"를 못 쓰고(`computed_depth`가 의미를 잃는다), 후보 평가치가 부하에 따라 흔들려 밴드 제어가 성립하지 않고, 상대의 강함이 서버 사정에 따라 달라진다.

**중간에 잘라 쓰는 것도 안 된다.** 잘린 결과는 depth N 결과가 아니라서, `computed_depth = N`으로 캐시에 적으면 거짓말이 되고 그 위에서 개입 판정이 돈다.

플레이어를 기다리게 해도 된다. 지연은 **캐시 · 선행 계산 · 깊이를 줄이는 것**(14→12)으로 잡는다. 그래서 `usi` 패키지에는 시간 기반 탐색 API가 아예 없다 — 없애는 것이 주석보다 확실하다.

## 검증

```sh
pnpm lint && pnpm typecheck && pnpm test && pnpm build   # 루트. 웹 전체 + 마크다운 포맷
cd apps/server && gofmt -l . && go vet ./... && go test -race ./...
terraform -chdir=infra fmt -check && terraform -chdir=infra validate
```

로컬에서 앱을 띄울 때는 `docker compose up -d`(api + db) 후 `pnpm dev`. **`../shogi` 컨테이너가 8080을 잡고 있으면 실패한다** — 그쪽을 먼저 내린다.

`-race`를 빼지 않는다. 엔진 프로세스와 세션 goroutine이 동시에 도는 구조라 데이터 경합이 가장 값비싼 버그이고, 그것만 도구가 사람보다 확실히 잘 잡는다.

## PR

**PR은 `/create-pr` 스킬로 만든다.**

`git-workflow:create-pull-request`는 회사(Ecube Labs) 템플릿이라 Jira 링크와 한글 섹션이 들어간다. **이 레포에서는 쓰지 않는다.**

**문서 수정이나 사소한 변경은 별도 PR로 쪼개지 않는다.** 작업 중에 나온 문서·설정 수정은 진행 중인 PR에 같이 넣는다. 혼자 쓰는 레포에서 PR을 나누는 이득보다 따로 머지하는 비용이 크다.

**작업 브랜치를 여러 개 만들지 않는다.** 브랜치를 나누면 아직 머지되지 않은 설정이 다른 브랜치에 없어서, `git add -A`가 무시돼야 할 파일을 커밋한다.

## Git 금지 사항

**머지하지 않는다.** `main` 머지는 사람이 GitHub PR 화면에서 직접 한다. PR을 여는 데까지가 Claude의 끝이다. 머지 여부를 묻지도 않고, PR URL만 알려주고 멈춘다.

- `gh pr merge` 금지
- `main`으로의 `git merge` · `git push` 금지
- `git-workflow:merge-to-develop` 금지 (회사 워크플로우. 이 레포엔 `develop`이 없다)

**force push하지 않는다.** `--force`도 `--force-with-lease`도 안 된다. 이미 푸시한 커밋을 되돌릴 때는 **`git revert`로 새 커밋을 쌓는다.**

**`main`에서 커밋하지 않는다.** 작업은 항상 브랜치를 파서 한다.

강제는 **GitHub 룰셋**이 한다. public 레포라 서버에서 브랜치 보호를 걸 수 있고, 그쪽은 `--no-verify`로 뚫리지 않는다. 룰셋이 막는 것: `main` 직접 푸시, `main` 삭제, 모든 force push, CI(`Server` / `Web`) 통과 전 머지.

로컬에는 `.githooks/pre-commit` 하나만 둔다. `main` 커밋을 막고 스테이징된 파일을 포맷한다.

클론마다 한 번:

```sh
git config core.hooksPath .githooks
git config pull.ff only
```

## 커밋

- Conventional Commits (`type: description`)
- **메시지는 영어.** 본문은 `-` 불릿으로 최대 4줄
- 브랜치는 `<type>/<kebab-case>`. 항상 최신 `main`에서 분기

## 포맷

**TS·마크다운은 oxfmt, Go는 gofmt.** Prettier를 쓰지 않는다 — 전역 VS Code 설정이 Prettier(100칸)를 기본 포매터로 두면 oxfmt(120칸)와 매번 싸운다. `.vscode/settings.json`이 이 워크스페이스에서만 포매터를 덮어쓰고, `pre-commit`이 스테이징된 파일을 한 번 더 정리한다.

**표의 파이프가 원문에서 어긋나 보이는 것은 정상이다.** oxfmt가 CJK 문자 폭을 1칸으로 세기 때문이고, 렌더 결과는 정상이다. **손으로 맞추지 않는다** — 다음 저장에 다시 틀어진다.

## 비밀

`.env`는 커밋하지 않는다. **레포가 퍼블릭이다.** 특히 조심할 것:

- OrcaRouter API 키, Google OAuth 클라이언트 시크릿
- Terraform state — DB 비밀번호가 평문으로 들어간다 (그래서 S3 백엔드에 둔다)
- `../shogi`에서 파일을 가져올 때 `server/.env` · `deploy/.env.prod` · `terraform.tfstate`가 딸려오지 않게 할 것

## 언어 — 사용자에게 보이는 것은 전부 일본어

**타깃 사용자는 일본인이다.** 한글이 한 글자라도 화면에 나가면 그 자리에서 "번역이 덜 된 앱"이 된다.

| 일본어                                  | 한글                        | 영어           |
| --------------------------------------- | --------------------------- | -------------- |
| UI 문자열 전부 (버튼·라벨·에러·빈 상태) | 문서 본문 (`docs/`, README) | 커밋 메시지    |
| LLM 프롬프트와 그 출력                  | 코드 주석                   | PR 본문        |
| RAG 코퍼스(`kb_chunks`)                 |                             | 변수·함수 이름 |
| 블런더 설명 템플릿, 캐시된 문구         |                             |                |
| 대외용 README, Zenn 기사                |                             |                |

- **LLM에 한글을 넣지 않는다.** 프롬프트가 한글이면 출력도 한글로 새고, `temperature=0` 캐시 키까지 갈라진다
- **`kb_chunks`에 한글을 넣지 않는다.** 일본어 질의로 검색하는데 코퍼스가 한글이면 임베딩 유사도가 무너진다
- **`../shogi/src/data`는 한 줄도 쓰지 않는다.** 원전이 개인 블로그이고 手筋 208문은 시판 서적 디지털화다. 퍼블릭 레포에서는 저작권 문제이고, 검증 안 된 설명을 초심자에게 내보내는 것은 이 제품의 존재 이유와 정면으로 어긋난다
- 쇼기 용어는 문서에서도 일본어 원문 그대로 쓴다 (持ち駒, 成り, 詰み). 한글로 옮기면 검색이 안 되고 원전과 대조가 안 된다

## 문서

- 확인하지 못한 항목은 추정으로 메우지 말고 `[미확정]`으로 표시
