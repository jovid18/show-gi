# 부하 회차 도구

**동시에 몇 판까지 되나**를 재는 자리다([06-status.md](../../docs/06-status.md) §5의 열린 물음). 근거와 결정은 [journal §103](../../docs/journal/101-120.md).

k6 를 쓴다. **규칙 엔진이 필요 없다** — 서버가 스냅샷마다 `legalMoves` 를 통째로 주므로 그중 하나를 뽑으면 판이 진행된다.

```
run.sh       회차를 거는 자리. 손잡이를 KEY=VALUE 로 받는다
main.js      회차의 입구. MODE 로 시나리오를 고른다
engine.js    엔진 대국 (/ws/game, 쿠키는 선택)
match.js     대인전 (/api/queue → /ws/match, 로그인 필요)
lib/         손잡이·지표·쿠키·수 고르기
seed.sql     provider='loadtest' 사용자와 레이팅
cleanup.sql  회차가 끝나면 돌린다 (한 줄)
```

## 준비

```sh
brew install k6

# 사용자를 만든다. 번호가 마지막에 찍힌다 — 그 목록을 LT_UIDS 로 넘긴다
docker exec -i show-gi-db psql -U showgi -d showgi -v ON_ERROR_STOP=1 < tools/loadtest/seed.sql
```

**사람 수와 레이팅 폭은 `seed.sql` 안의 숫자 둘이다.** 돌리기 전에 그 자리를 고친다 — 주석 `①`·`②` 가 가리킨다. **명령줄 변수를 안 쓴다**: 프로덕션은 사람이 DataGrip 으로 도는 자리인데 psql 전용 문법(`:n`·`\if`)이 거기서 다르게 읽혀 한 번 사용자가 안 늘었다([journal §108](../../docs/journal/101-120.md)).

**레이팅은 기본이 전원 1500이다**(척도의 중앙). 그러면 짝이 즉시 잡혀서 「대인전이 도는가」를 보기에 좋다. 밴드가 넓어지는 것을 재려면 `②` 를 700 으로 두는데, 그 회차는 사람이 둘이다 — 이유는 `seed.sql` 주석에 계산으로 적혀 있다. **척도 밖의 값을 심지 않는다**: 8000점짜리 행은 지금 새는 곳이 없어도 순위표를 붙이는 날 오염원이 된다.

**쿠키는 도구가 굽는다.** 세션은 서버에 짝이 없는 서명 쿠키라(`internal/auth`) `SESSION_SECRET` 만 있으면 Google 왕복 없이 같은 값을 만들 수 있다. 프로덕션 회차에서는 SSM 에서 그때 읽어 환경변수로만 넘긴다 — 디스크에 안 남긴다.

**엔진 대국에도 쿠키를 단다.** `/ws/game` 은 익명으로도 열리지만, 그러면 `games.user_id` 가 NULL 이라 **회차가 만든 판과 실제 익명 사용자의 판을 구별할 수 없고** `cleanup.sql` 이 그 판에 닿지 않는다. `SESSION_SECRET` 과 `LT_UIDS` 가 둘 다 있으면 붙고, 없으면 익명으로 돌면서 `setup` 이 경고한다.

```sh
export SESSION_SECRET=$(aws ssm get-parameter --name /show-gi/prod/SESSION_SECRET \
  --with-decryption --region ap-northeast-1 --profile show-gi \
  --query Parameter.Value --output text)
```

**`run.sh --prod` 는 이 조회를 스스로 한다.** 손으로 `export` 하는 것은 `main.js` 를 직접 부를 때만 필요하다.

## 돌리는 법

**`run.sh` 로 건다.** 손잡이는 그대로 `KEY=VALUE` 로 주고, 스크립트가 레포 루트를 스스로 찾는다.

```sh
# 엔진 대국만. 쿠키를 달면 그 판들을 나중에 지울 수 있다
tools/loadtest/run.sh MODE=engine VUS_ENGINE=3 DURATION=3m LT_UIDS=5457,5458,5459

# 대인전만
tools/loadtest/run.sh MODE=match VUS_MATCH=4 LT_UIDS=5457,5458,5459,5460

# 둘을 동시에 — 서로 굶히는지를 재는 회차다
tools/loadtest/run.sh MODE=both VUS_ENGINE=3 VUS_MATCH=4 LT_UIDS=…

# 프로덕션. BASE 와 SESSION_SECRET 을 스크립트가 채운다
tools/loadtest/run.sh --prod MODE=match VUS_MATCH=12 THINK_MS=3000 DURATION=15m LT_UIDS=…
```

**기본값을 `run.sh` 에 두지 않는다.** k6 스크립트가 이미 든다 — 손잡이 대부분이 `lib/config.js` 이고 시나리오 넷(`MODE`·`DURATION`·`VUS_ENGINE`·`VUS_MATCH`)이 `main.js` 다. 한 벌 더 두면 한쪽을 고칠 때 다른 쪽이 조용히 낡아 「무슨 값으로 잰 회차인가」가 갈린다. 그래서 로그 첫 줄도 **값이 있는 손잡이만** 적는다 — 인자로 준 것과 밖에서 `export` 한 것을 같이 읽고, `SESSION_SECRET` 은 목록에 아예 없다.

**원격에는 `SESSION_SECRET` 도 있어야 한다.** `--prod` 면 스크립트가 SSM 에서 읽고, 아니면 직접 넘겨야 한다 — 쿠키가 없으면 `MODE=engine` 이 경고만 하고 익명으로 돌아서 아래 가드가 그대로 뚫린다.

**익명 판 가드는 플래그가 아니라 주소가 건다.** `--prod` 없이 `BASE` 만 원격으로 넘겨도 같은 곳에 걸리므로, 로컬이 아닌 주소에는 `LT_UIDS` 에 **양의 정수가 하나라도** 있어야 한다 — 비어 있는 것뿐 아니라 `LT_UIDS=abc` 도 막는다(`lib/config.js` 가 숫자 아닌 값을 걸러 버려 빈 목록과 같아진다).

`k6 run tools/loadtest/main.js` 를 직접 불러도 같다. 다만 손잡이를 env 접두사로 붙여야 하고 cwd 가 레포 루트여야 하는데, **Claude 가 도는 세션에서는 그 모양이 `Bash(k6 run:*)` 허용 규칙에 안 걸린다** — 규칙이 접두사 매칭이라 `BASE=…` 로 시작하는 명령을 못 덮고, `$(aws ssm …)` 의 명령 치환도 마찬가지다. 그때는 판정이 분류기로 넘어가 같은 종류의 회차가 한 번은 통과하고 한 번은 막힌다.

| 환경변수                               | 기본값                  | 무엇                                                                            |
| -------------------------------------- | ----------------------- | ------------------------------------------------------------------------------- |
| `BASE`                                 | `http://localhost:8080` | WS 주소는 여기서 만든다                                                         |
| `MODE`                                 | `engine`                | `engine` · `match` · `both`                                                     |
| `VUS_ENGINE` · `VUS_MATCH`             | 3 · 2                   | **VU 하나가 사람 하나**다. 엔진은 1 VU 가 판 하나, 대인전은 **2 VU 가 판 하나** |
| `DURATION`                             | `3m`                    | 회차 길이                                                                       |
| `MAX_PLIES`                            | 100                     | 手数 상한. 넘으면 投了한다. 60은 終盤에 못 갔다                                 |
| `THINK_MS`                             | 0                       | 한 手 전에 기다릴 시간. **대인전은 켠다**                                       |
| `SEED`                                 | 1                       | 같은 씨앗이 같은 수순을 준다                                                    |
| `LT_UIDS`                              | —                       | 회차가 쓸 사용자 번호. **VU 합보다 많아야 한다**                                |
| `HANDICAP`                             | —                       | `/api/handicaps` 의 id                                                          |
| `GAME_TIMEOUT_MS` · `STALL_TIMEOUT_MS` | 900000 · 90000          | 멈춤을 잡는 것은 STALL. GAME 은 보루라 안 터져야 한다                           |
| `QUEUE_TRIES` · `QUEUE_INTERVAL`       | 30 · 2                  | 대기열을 몇 번, 몇 초마다 물어보나                                              |

**VU 하나가 사람 하나다.** 이 앱의 부하 단위가 초당 도착률이 아니라 동시 판수라서 `constant-vus` 로 둔다 — 판 하나가 연결 하나를 몇 분 잡는다.

**엔진 대국은 1 VU 가 판 하나이고 대인전은 2 VU 가 판 하나다**(`match.js` 가 사람마다 큐에 선다). 그래서 대인전 6판은 `VUS_MATCH=12` 이고, 이 자리를 판수로 읽어 절반짜리 회차를 한 번 걸었다([journal §113](../../docs/journal/101-120.md)). `LT_UIDS` 는 두 시나리오의 VU 합보다 많아야 한다.

**`THINK_MS` 를 안 켜면 VU 하나가 사람 하나보다 크다.** 도구는 자기 차례가 된 그 순간 두므로 생각 시간이 0이다. 사람이 한 수에 T초를 쓰면 엔진 수요가 `2.15/(2.15+T)` 로 줄고(2.15초는 동시 1판에서 잰 착수 왕복 평균, [journal §104](../../docs/journal/101-120.md)), **T=3초면 VU 8 이 사람 20명쯤**이다. 그래서 그때 VU 수는 **보수적 상한**으로 읽는다 — VU N 에서 버티면 사람 N 명에서는 확실히 버틴다. 거꾸로는 아니다: 사람 20명은 순간적으로 동시 8판을 넘는 일이 생긴다.

**켜면 그 환산이 없어지고 VU 수가 곧 사람 수다.** 환산 없이 읽는 쪽이 나으므로 새 회차는 켠다.

**대인전에는 켜야 한다.** 양쪽이 다 도구라서 기다릴 상대가 없고, 그래서 100手 판이 왕복 속도로 2초에 끝난다 — 사람 넷이 도착률 37판/분을 만들고 그것이 사후 분석의 배수구(0.64판/분)를 58배 넘긴다([journal §105](../../docs/journal/101-120.md)). **VU 를 낮추는 것으로는 안 맞는다**: 어긋난 폭이 배수로 줄일 값이 아니다.

**엔진 대국에는 없어도 된다.** 엔진의 탐색 시간이 저절로 페이서가 되어 도구가 상대를 기다린다.

**켠 회차에서는 `move_cycle` 에서 `THINK_MS` 를 뺀다.** 그 지표는 보낸 뒤 다시 내 차례가 될 때까지이고 그 사이에 상대의 생각 시간이 들어 있다 — 3초로 잰 회차의 중앙값이 3.03초이고 서버 몫이 30ms 다.

**「깨질 때까지」 올린다.** 회차를 시작하기 전에 깨짐 정의를 적고, 그 신호가 나올 때까지 계단을 만든다 — 5xx·panic 1건 이상 · WS 연결 실패 · `cut=stall` 이 판의 절반 이상. 신호가 0이었던 아래 계단은 다음 회차에서 뺀다. **`GAME_TIMEOUT_MS` 를 판 길이보다 넉넉히 크게 두고, `DURATION` 은 판이 몇 개 완주할 만큼 길게 잡는다** — 시한이 판 길이에 가까우면 `cut=capped` 가 저절로 섞여서 신호를 흐린다. 8판 동시의 100手 판이 7분쯤이므로 계단마다 15분이 필요하다.

**계단 사이에 큐를 비운다.** 앞 계단의 분석 백로그와 열린 세션이 남은 채로 다음을 걸면 그 부하가 섞여서 어느 계단의 숫자인지 알 수 없다. 대시보드의 `AnalysisBacklogPlies` 와 `WsSessionsActive` 가 **0으로 돌아온 뒤에** 다음 계단으로 간다 — 지표 창도 같이 넘어온다(5분 p95 알람이 앞 계단 때문에 울린 적이 있다, [journal §104](../../docs/journal/101-120.md)).

**배수는 이제 몇 분이다.** 8판 15분 회차 뒤에 백로그 771 → 0 이 **5분**이었다(≈154手/분, [journal §113](../../docs/journal/101-120.md)). [journal §105](../../docs/journal/101-120.md)의 「100분」은 `THINK_MS=0` · 워커 하나였을 때의 값이라 지금 조건에 안 걸린다 — 먼저 기다려 보고, 안 빠질 때만 재배포로 비운다:

```sh
aws ecs update-service --region ap-northeast-1 --cluster show-gi --service show-gi \
  --force-new-deployment --profile show-gi
```

**단절이 생긴다** — `desiredCount=1` · `maximumPercent=100` 이라 낡은 태스크를 먼저 세우고, healthz 가 503 인 창이 약 50초였다. 잃는 것은 그 회차가 만든 판의 평가치뿐이라 부하 회차에서는 값싸다. **실사용자의 판에는 같은 동작이 사고다**(같은 곳 ⑥).

## 도구가 재는 것

서버 지표와 겹치지 않는 것만 둔다. 겹치면 두 숫자가 갈릴 때 어느 쪽이 맞는지 정할 수 없다.

| 지표                      | 무엇                                                                                                                |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `showgi_move_cycle`       | 내 수를 보내고 다시 내 차례가 되기까지. **사람이 기다리는 것**                                                      |
| `showgi_queue_wait`       | 짝이 잡히기까지                                                                                                     |
| `showgi_games` · `_plies` | 판 수와 手数                                                                                                        |
| `showgi_interventions`    | 개입 건수 (카테고리 라벨)                                                                                           |
| `showgi_rejects`          | **0이어야 한다.** 하나라도 있으면 도구 버그다                                                                       |
| `showgi_stalls`           | 시한이 끊은 판. **`cut` 으로 갈린다** — `stall`(서버 무응답)만 깨짐 신호이고 `capped`(우리 手数·시간 상한)는 아니다 |

**`MODE=both` 은 이 넷을 시나리오별로도 찍는다** — `showgi_move_cycle` · `_games` · `_plies` · `_stalls` 아래에 `{ scenario:engine }` · `{ scenario:match }` 줄이 붙는다. 합친 값 하나로는 「둘이 서로 굶히나」에 답할 수 없어서다([journal §113](../../docs/journal/101-120.md)).

**요약의 THRESHOLDS 에 늘 통과하는 줄이 여덟 개 생기는 것은 정상이다.** k6 가 하위 지표를 임계치로 선언해야 요약에 넣으므로 그것을 선언으로 쓴 것이고, 게이트가 아니다. 깨짐을 보는 것은 `showgi_rejects` 와 `http_req_failed` 둘뿐이다.

서버 쪽은 [CloudWatch 대시보드](../../docs/journal/101-120.md) 한 장으로 본다. 회차에서 제일 먼저 볼 것은 **`EnginePoolWaitGameSeconds`** 다 — 분석·검토가 섞이지 않은 대국의 대기이고, 그것이 튀면 사람이 기다린 것이다.

## 프로덕션에 걸 때

- **레이트 리밋이 아무 데도 없다**(Caddy·ALB·앱). 막아 줄 것이 없으므로 `http_req_failed` 임계에 `abortOnFail` 이 걸려 있다 — 5xx 가 5%를 넘으면 회차가 스스로 멈춘다
- **태스크가 한 대다.** 부하가 곧 실사용자 장애다
- 알람이 울리고 메일이 간다. 회차 시각을 저널에 적어 둔다 — 대인전 회차는 `show-gi-analysis-backlog` 이 걸리는 자리다([journal §108](../../docs/journal/101-120.md))
- 끝나면 `cleanup.sql`. `users` 한 줄을 지우면 판·기보·평가치·레이팅·큐가 CASCADE 로 같이 사라진다
- **`LT_UIDS` 없이 걸지 않는다.** 익명 판은 그 정리에 안 걸린다 — `run.sh` 가 원격 주소면 그 자리를 막는다

**부하와 지연을 같은 회차에서 재지 않는다.** 용량은 도쿄 하나에서 계단식으로, 지연은 도쿄+오사카에서 낮은 부하로 — 포화된 서버에서는 리전 간 10ms 차이가 엔진 대기에 삼켜진다.

## 남은 회차

[journal §104](../../docs/journal/101-120.md) 가 엔진 대국 계단을, [§105](../../docs/journal/101-120.md) 가 대인전 계단을 끝냈다. 순서는 위에서부터다 — 앞의 것이 뒤의 것의 기준선을 만든다.

| 회차                                 | 설정                                                      | 무엇을 답하나                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------ | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ~~**깨질 때까지**~~                  | `MODE=engine`, 4·6·8판                                    | **돌았다**([journal §109](../../docs/journal/101-120.md)). `c6g.large` 에서 **4판 지속 가능 · 6판 포화**다(풀 대기 p95 3초 기준). **정착 구간으로만 읽는다** — 앞 2\~3분은 캐시가 데워져 있어 마이크로초다                                                                                                                                                                                                                                                                                                                     |
| ~~**엔진 풀을 넓혀 본다**~~          | `ENGINE_POOL_SIZE` 4                                      | **돌았다**([journal §110](../../docs/journal/101-120.md)). **풀은 레버가 아니다** — 엔진 6판에서 풀 대기 p95 가 8.52→3.48초로 내려가는 대신 탐색 p95 가 7\~20초로 오르고 CPU 는 99%에서 99%다. 빌린 구간이 탐색 하나뿐이라 슬롯을 코어보다 많이 줘도 같은 CPU 를 잘게 쪼갠다. 2로 되돌렸다                                                                                                                                                                                                                                     |
| ~~**엔진 대국 기준선 (사람 속도)**~~ | `MODE=engine`, `THINK_MS=3000`                            | **돌았다**([journal §123](../../docs/journal/121-140.md)). **6판이 선에 걸친다** — 풀 대기 p95 가 13분 중 1분만 3초를 넘는다. `THINK_MS=0` 에서 6판이 포화였던 것과 갈린다(생각 시간을 켜면 판당 엔진 수요가 준다). 다만 stall 2건에 `move_cycle` max 24.9초라 「지속 가능」으로는 안 적는다. **백로그가 15분 내내 0** — 엔진 대국은 사후 분석에 일을 안 만들므로 이 행은 분석 대수와 무관하다                                                                                                                                 |
| ~~**비버스터블 박스에서 재측정**~~   | 위와 같은 계단, `c6g.large`                               | **돌았다**([journal §109](../../docs/journal/101-120.md)). **6판 지속 가능 · 8판 포화**가 표의 첫 행이다. 절벽은 없어졌는데 천장은 그대로다 — 같은 Graviton2 2 vCPU 라서다                                                                                                                                                                                                                                                                                                                                                     |
| ~~**대인전**~~                       | `MODE=match`, 4판                                         | **돌았다**([journal §105](../../docs/journal/101-120.md)). 짝은 2초에 잡히고 레이팅·실력 추정이 실물로 돈다. 새로 나온 것은 **사후 분석의 배수구가 0.64판/분**이라는 것이다                                                                                                                                                                                                                                                                                                                                                    |
| ~~**대인전 (사람 속도)**~~           | `MODE=match`, `THINK_MS=3000`                             | **돌았다**([journal §106](../../docs/journal/101-120.md)). 4판(8명)에서 버티고 6판(12명)에서 포화. **병목은 CPU 가 아니라 분석 워커 하나**라 이 값은 박스의 절반을 잰 것이다                                                                                                                                                                                                                                                                                                                                                   |
| ~~**워커를 늘린 뒤 재측정**~~        | 위와 같은 계단                                            | **돌았다**([journal §107](../../docs/journal/101-120.md)). 12판 계단이 뚫렸고(백로그 443→13) 사람의 풀 대기는 표본 0이다. **그 위를 못 걸었다** — 스팟이 회차를 끊었다                                                                                                                                                                                                                                                                                                                                                         |
| ~~**천장 찾기**~~                    | `MODE=match`, 16 VU                                       | **돌았다**([journal §108](../../docs/journal/101-120.md)). **8판(16명)이 포화**다 — 백로그가 1 → 756 으로 단조 증가했고 5xx·거절·드롭은 0이다. 천장은 6판과 8판 사이. **그런데 잰 것이 박스가 아니라 크레딧 절벽이었다** — 7분에 크레딧이 말라 탐색이 0.24초에서 8.52초가 됐다                                                                                                                                                                                                                                                 |
| **밴드**                             | `MODE=match` 2판, **`seed.sql` 을 사람 둘 · 폭 700 으로** | 밴드가 대기로 넓어지는가 — 1150·1850 이 17초에 붙는다. **n=2 가 상한이다**: 최소 쌍거리가 360(처음 밴드)보다 커야 하고 `(n-1)*spread` 가 800(시드 폭)을 넘으면 안 되는데, 둘을 같이 만족하는 n 이 2뿐이다                                                                                                                                                                                                                                                                                                                      |
| ~~**섞어서**~~                       | `MODE=both`                                               | **돌았다**([journal §109](../../docs/journal/101-120.md)). 엔진 4판 + 대인전 6판에서 `borrower` 가 처음 갈렸다 — 대인전은 `move_cycle` p95 3.06초로 안 밀리고 엔진 대국이 p95 9.10초를 먹는다. 분석은 뒤로 밀려 백로그 727                                                                                                                                                                                                                                                                                                     |
| ~~**섞어서 — 사람 수 계단**~~        | `MODE=both`, `THINK_MS=3000`, 4·8·12명                    | **돌았다**([journal §114](../../docs/journal/101-120.md)). **8명 지속 가능 · 12명 포화**이고 그 자리가 엔진의 단독 선과 같다. **대인전 착수는 어느 계단에서도 안 굶는다**(p95 3.04·3.05·3.06초) — 그 경로가 엔진을 안 지나서다. **엔진이 포화하면 대인전 사후 분석이 같이 무너진다**(대인 3판인데 백로그 866). 반대는 안 보인다 **[§123](../../docs/journal/121-140.md)이 그 문장을 접었다** — 티어를 가른 뒤 같은 12명 계단에서 백로그 최고가 **866 에서 3**이다. 선은 그대로 12명 포화이고, 무너지는 층이 엔진 하나로 줄었다 |
| ~~**詰み 캐시 — 대인전**~~           | `MODE=match`, `THINK_MS=3000`                             | **돌았다**([journal §113](../../docs/journal/101-120.md)). 히트율이 **정상 구간 8.0%(6판) · 포화 20.2%(8판)** 이고 **용량표는 안 움직였다**(6판 지속 · 8판 포화 그대로). 히트율을 정하는 것은 국면이 아니라 소비자 수다 — 그쪽은 사후 분석 하나가 한 번 묻는다. 그 호출은 낭비가 아니다: 詰み 판정이 실력 추정의 終盤 눈이다                                                                                                                                                                                                   |
| ~~**분석 대수 계단**~~               | `MODE=match`, 16 VU, 분석 대수 1·2                        | **돌았다**([journal §121](../../docs/journal/121-140.md)). 같은 8판 부하에 대수만 갈랐다 — **1대는 백로그 최고 284 에 9분째부터 안 내려오고, 2대는 최고 12 에 추세가 없다**다. **티어를 가른 것만으로는 안 풀린다**: 1대가 §119 의 `both` 한 대(269)와 같은 자리다. **2대의 천장은 [journal §122](../../docs/journal/121-140.md)가 쟀다** — 12판(24명) 지속 · 14판(28명) 포화이고, 지속선이 6판에서 12판으로 두 배다. **처리량은 `EngineSearches` 합으로 안 보인다** — 캐시가 덮어서 합계 +11% 이고 캐시를 뺀 값이 +46% 다     |
| ~~**오토스케일**~~                   | `MODE=match`, 24 VU, 35분                                 | **돌았다**([journal §124](../../docs/journal/121-140.md)). **알람이 대수를 돌렸다** — 12판에서 밀린 手가 임계 100 을 5분 넘기자 서비스 `desired_count` 가 1에서 2로 오르고, **알람에서 태스크 가동까지 2분 35초**다. 밀린 手가 대가 붙은 분에 **1,089 로 정점**을 찍고 10분 만에 255 가 된다. **계단은 10분보다 길어야 한다** — 대인전 백로그는 판이 처음 끝나는 자리까지 아무것도 안 보여 준다(8판 계단을 9분에 접었다가 결론을 못 냈다). `move_cycle` p95 3.03초 · 거절·5xx·버린 판 0                                        |
| **짧은 판**                          | `MODE=engine`, `MAX_PLIES=24`                             | 완주율을 올리면 **분석 큐가 밀리나** — 엔진 대국에서도 그런가. 대인전에서는 밀렸다([journal §105](../../docs/journal/101-120.md))                                                                                                                                                                                                                                                                                                                                                                                              |
| **지연**                             | 도쿄 + 오사카, 낮은 부하                                  | 리전 간 차. **러너 EC2 가 선행**이고, operator IAM 에 VPC 생성과 SSM Run Command 가 없어서 정책 확장부터다                                                                                                                                                                                                                                                                                                                                                                                                                     |
