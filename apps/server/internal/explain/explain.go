// Package explain 은 **이미 정해진 판단을 일본어 문장으로 바꾼다.**
//
// 여기서 판단하지 않는다. 무엇이 블런더이고 왜 그런지는 `intervene` 과 `game` 이 결정적
// 룰과 엔진 평가치로 이미 정했고, 이 패키지에 오는 것은 그 결과뿐이다 — 판도 SFEN도 cp도
// 들어오지 않는다.
//
// **문장은 결정적이다.** 같은 사실에는 언제나 같은 문장이 나가고, 그 문장은 `Render` 와
// `RenderSummary` 가 코드로 만든다 — 사실을 지어낼 자리가 없다는 것이 이 패키지의 전부다.
// 학습 앱에서 설명이 틀리면 초심자는 검증할 능력이 없어 틀린 것을 그대로 배운다(CLAUDE.md).
//
// 입력을 좁히는 것이 곧 안전장치다. 카테고리마다 **말할 수 있는 사실**이 다르고, 그 목록은
// `Facts.used` 한 곳에 있다 — 문구를 고치는 쪽도 사실을 채우는 쪽도 같은 함수를 지난다.
package explain
